<div class="cocktail-form" >
<%= simple_form_for [@cocktail] do |f| %>
<%= f.input :name %>
<%= f.input :photo, as: :file %>
<%= f.submit "Add Cocktail", class: "button-ingredient"%>
<% end %>
</div>
